name: BigBasket Stock Checker

on:
  # Keep your existing schedules
  schedule:
    - cron: "*/10 * * * *"      # every 10 minutes
    - cron: "5-59/10 * * * *"   # every 10 minutes offset by 5 minutes
  # Add workflow_dispatch to allow external/API triggers
  workflow_dispatch:

jobs:
  run-tracker:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 🌐 Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Run tracker
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID_1: ${{ secrets.CHAT_ID_1 }}
          CHAT_ID_2: ${{ secrets.CHAT_ID_2 }}
        run: |
          echo "Workflow triggered at: $(date)"  # log timestamp for verification
          python tracker.py
